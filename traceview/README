To automate the lanuch and termination of trace collection in Traceview, run the following command:


To parse app-level function call traces generated by TraceView, run dmtracedump tools with following command:
dmtracedump -o $input_file > $output_file

To build dmtracedump tool, compile Tracedump.c and Profile.h with the following command:
gcc -o dmtracedump TraceDump.c 
